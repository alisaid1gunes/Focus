const getOne = require('./getOne');

const getAll = require('./getAll');

const save = require('./save');

const update = require('./update');

const remove = require('./remove');

module.exports = { getAll, update, remove, getOne, save };
